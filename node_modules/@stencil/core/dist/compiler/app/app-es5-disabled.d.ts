import { CompilerCtx, Config, OutputTarget } from '../../declarations';
export declare function generateEs5DisabledMessage(config: Config, compilerCtx: CompilerCtx, outputTarget: OutputTarget): Promise<string>;
