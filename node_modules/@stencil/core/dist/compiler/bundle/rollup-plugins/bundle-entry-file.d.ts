import * as d from '../../../declarations';
export default function bundleEntryFile(config: d.Config, buildCtx: d.BuildCtx, entryModules: d.EntryModule[]): {
    name: string;
    resolveId(importee: string): string;
    load(id: string): string;
};
export declare function createEntryPointString(config: d.Config, entryModule: d.EntryModule): string;
