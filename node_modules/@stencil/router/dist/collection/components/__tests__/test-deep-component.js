import injectHistory from '../../global/injectHistory';
export class TestDeepComponent {
    componentDidUpdate() {
        console.log('deepchild DidUpdate');
    }
    render() {
        return (h("div", null,
            h("pre", null,
                h("b", null, "this.history"),
                ":",
                h("br", null),
                JSON.stringify(this.history, null, 2)),
            h("pre", null,
                h("b", null, "this.history"),
                ":",
                h("br", null),
                JSON.stringify(this.location, null, 2)),
            h("button", { onClick: () => this.history.push('/') }, " Back Home")));
    }
    static get is() { return "test-deep-component"; }
    static get properties() { return {
        "blue": {
            "elementRef": true
        },
        "history": {
            "type": "Any",
            "attr": "history"
        },
        "location": {
            "type": "Any",
            "attr": "location"
        }
    }; }
}
injectHistory(TestDeepComponent);
