export function invariant(value, ...args) {
    if (!value) {
        console.error(...args);
    }
}
export function warning(value, ...args) {
    if (!value) {
        console.warn(...args);
    }
}
