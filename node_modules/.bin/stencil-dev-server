#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@stencil/dev-server/bin/stencil-dev-server" "$@"
  ret=$?
else 
  node  "$basedir/../@stencil/dev-server/bin/stencil-dev-server" "$@"
  ret=$?
fi
exit $ret
